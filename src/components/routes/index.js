export const LogAndReg = {
    login: '/login',
    register: '/register'
}

export const Client = {
    client: '/client',
    add: '/add'
}
export const Admin = {
    admin: '/admin',
    myclients: '/myclients',
    waiting: '/waiting',
}
const Routes = {
    LogAndReg,
    Client,
    Admin
}

export default Routes;